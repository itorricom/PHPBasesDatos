…or 
create a new repository on the command line
echo "# PHPBasesDatos" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/itorricom/PHPBasesDatos.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/itorricom/PHPBasesDatos.git
git branch -M main
git push -u origin main

````
Driver mysqli únicamente soporta conexiones con gestores de base de datos MySQL, 
Para conexión con otros gestores como por ejemplo PostgreSQL tenemos PDO.

````
